# Движок white (белый фон) — обязательный минимум
pillow>=10.0
numpy>=1.24
scipy>=1.10

# MCP-сервер для Claude Code
mcp

# Движок ai (любой фон) — опционально, ~100 МБ колёс + ~180 МБ модель
# rembg
# onnxruntime
