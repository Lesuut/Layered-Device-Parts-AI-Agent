# The white engine (white background) — the mandatory minimum
pillow>=10.0
numpy>=1.24
scipy>=1.10

# MCP server for Claude Code
mcp

# The ai engine (any background) — optional, ~100 MB of wheels + a ~180 MB model
# rembg
# onnxruntime
