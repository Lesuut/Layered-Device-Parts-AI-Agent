CAMERA: strict orthographic top-down view, 90° directly overhead, like a flatbed scanner. NOT isometric, NOT 3D, NOT tilted, NOT the angle from the reference image. Every part must be perfectly flat, straight-on, zero perspective, zero foreshortening. This is the most important rule — ignore the reference image's camera angle completely and replace it with a flat top-down view, like Image 2.

Take every component from the reference exploded diagram and lay it out flat on a pure white background (
#FFFFFF), each part separated, not touching, not overlapping.

Keep the exact same art style, linework, colors, and shading as the reference — just change the camera angle to flat top-down and rearrange the parts.

No shadows anywhere (no drop shadow, no ambient occlusion) — flat, evenly lit.

Show only the parts that exist in the reference, in the exact same quantity — do not add, invent, or duplicate screws/springs/buttons to fill space.

Back cover: show its INNER side (facing into the device), not the outer/exterior side.
Front shell and PCB: show their outer/working face, facing up, flat.

No text, no labels, no numbers, no watermark, no frame, no tray.

Every part must look like it was scanned flat from directly above — no tilt, no isometric angle, anywhere in the image.